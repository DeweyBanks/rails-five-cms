<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>body</th>
      <th>Created By: </th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.title %></td>
        <td><%=h truncate(post.body, :length => 300, :omission => ". . ." , :escape => false) %></td>
        <td><%=  %></td>
        <td><%= link_to 'Read More', post_path(post) %></td>
      </tr>
    <% end %>
  </tbody>
</table>


